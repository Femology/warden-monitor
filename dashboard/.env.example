# server-only -- read exclusively by src/app/api/explain/route.ts, never
# imported by any client component. Evomap's OpenAI-compatible endpoint,
# model evomap-deepseek-v4-flash, used only for the "Explain this" feature
# (Phase 18). If this is unset or the call fails for any reason, the route
# falls back to a pre-written explanation instead of erroring.
EVOMAP_API_KEY=
